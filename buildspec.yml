version: 0.2

env:
  secrets-manager:
    DATABASE_PASS: "my-app-secret:DB_PASSWORD"

phases:
  build:
    commands:
      - echo "Hello AWS"
      - echo "Build Successful"

artifacts:
  files:
    - '**/*'